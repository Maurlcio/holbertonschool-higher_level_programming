#!/usr/bin/node

function bigThree () {
  const COOL_LANGS = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

  for (let i = 0; i < COOL_LANGS.length; i++) {
    console.log(COOL_LANGS[i]);
  }
}

bigThree();
